[global]
env_allowed = ["HOME", "PATH"]
env_import = ["home=HOME", "path=PATH"]
vars = ["app_dir=/opt/myapp"]
env_vars = ["BASE_DIR=%{app_dir}"]

[[groups]]
name = "test_group"
vars = ["log_dir=%{app_dir}/logs"]
env_vars = ["LOG_DIR=%{log_dir}"]

[[groups.commands]]
name = "test_cmd"
vars = ["temp_file=%{log_dir}/temp.log"]
cmd = "/bin/echo"
args = ["%{temp_file}"]
